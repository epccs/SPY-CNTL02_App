# Empty

CubeMX has generated the Makefile and other files that I copied into the Core folder. Use this folder as a Minimal template to copy into a folder (at the same levle as this one) and start new firmware. Note: the Makefile has been changed to work with the shared drivers/linker script directory.


